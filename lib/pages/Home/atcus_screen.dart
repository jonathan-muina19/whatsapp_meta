import 'package:flutter/material.dart';

class AtcusScreen extends StatelessWidget {
  const AtcusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Actus'
        ),
      ),
    );
  }
}